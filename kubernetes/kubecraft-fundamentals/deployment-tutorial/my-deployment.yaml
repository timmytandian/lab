apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: my-depl
  name: my-depl
spec:
  replicas: 10
  selector:
    matchLabels:
      app: my-depl
  template:
    metadata:
      labels:
        app: my-depl
    spec:
      containers:
      - image: httpd
        name: httpd
      - image: redis:8.0-M04-alpine
        name: redis
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
